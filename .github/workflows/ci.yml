# STRICTLY A SOCIAL MEDIA PLATFORM
# Intellectual Property & Artistic Inspiration
# Legal & Ethical Safeguards

name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    uses: ./.github/workflows/base-tests.yml
    secrets: inherit
    with:
      check_patch: ${{ github.event_name == 'pull_request' }}
